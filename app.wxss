/* app.wxss - Fresh glass light theme */

page {
  background: #fffaf7;
  color: #4f4356;
  font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", "SF Pro Icons", "Helvetica Neue", "PingFang SC", sans-serif;
  -webkit-font-smoothing: antialiased;
}

view,
text,
button,
input,
textarea,
picker {
  box-sizing: border-box;
}

/* 全局变量定义区 */
.page {
  min-height: 100vh;
  position: relative;
  overflow: hidden;

  /* --- 颜色变量 --- */
  --bg-base: #fffaf7;
  --ink-primary: #4f4356;
  --ink-secondary: #6f6377;
  --ink-tertiary: #96889c;

  /* 柔和点缀色 */
  --neon-primary: #f39ab5;
  --neon-secondary: #8ecae6;
  --neon-accent: #f7c7a6;

  /* 轻玻璃材质 */
  --glass-bg: rgba(255, 255, 255, 0.62);
  --glass-border: rgba(255, 255, 255, 0.78);
  --glass-border-highlight: rgba(255, 255, 255, 0.92);

  /* 缓动曲线 */
  --apple-ease: cubic-bezier(0.25, 1, 0.5, 1);
  --fluent-ease: cubic-bezier(0.16, 1, 0.3, 1);

  /* 柔和阴影 */
  --glow-primary: 0 0 28px rgba(243, 154, 181, 0.18);
  --shadow-glass: 0 18px 42px rgba(214, 188, 205, 0.22);

  /* 背景渐变遮罩 */
  background:
    radial-gradient(circle at 12% 12%, rgba(243, 154, 181, 0.24), transparent 34%),
    radial-gradient(circle at 88% 18%, rgba(142, 202, 230, 0.22), transparent 30%),
    radial-gradient(circle at 50% 100%, rgba(247, 199, 166, 0.22), transparent 34%),
    linear-gradient(180deg, #fffaf7 0%, #fffefb 42%, #fdf7f5 100%);
}

/* 针对不同主题的光影变体 */
.theme-blush {
  --neon-primary: #ee9bb4;
  --neon-secondary: #f7c3a1;
  --neon-accent: #ffd8e6;
  background:
    radial-gradient(circle at top left, rgba(238, 155, 180, 0.26), transparent 34%),
    radial-gradient(circle at bottom right, rgba(247, 195, 161, 0.2), transparent 30%),
    linear-gradient(180deg, #fff8f8 0%, #fffdfb 45%, #fef6f6 100%);
}

.theme-ocean {
  --neon-primary: #89c8e8;
  --neon-secondary: #9fd8c3;
  --neon-accent: #d8eff8;
  background:
    radial-gradient(circle at top left, rgba(137, 200, 232, 0.22), transparent 34%),
    radial-gradient(circle at bottom right, rgba(159, 216, 195, 0.22), transparent 30%),
    linear-gradient(180deg, #f6fbff 0%, #fcfffe 45%, #f4fbf8 100%);
}

.ambient-orb {
  position: absolute;
  border-radius: 50%;
  filter: blur(120px);
  opacity: 0.42;
  pointer-events: none;
  z-index: 0;
  mix-blend-mode: multiply;
  animation: fluent-flow 20s var(--apple-ease) infinite alternate;
  will-change: transform;
}

.orb-1 {
  animation-duration: 24s;
}

.orb-2 {
  animation-duration: 28s;
}

.orb-3 {
  animation-duration: 32s;
}

@keyframes fluent-flow {
  0% { transform: translate3d(0, 0, 0) scale(1) rotate(0deg); }
  26% { transform: translate3d(22rpx, -28rpx, 0) scale(1.05) rotate(4deg); }
  58% { transform: translate3d(-18rpx, 34rpx, 0) scale(0.96) rotate(-3deg); }
  100% { transform: translate3d(14rpx, -12rpx, 0) scale(1.03) rotate(3deg); }
}

.glass-card {
  position: relative;
  z-index: 10;
  background: var(--glass-bg);
  border: 1px solid var(--glass-border);
  backdrop-filter: blur(34px) saturate(145%);
  -webkit-backdrop-filter: blur(34px) saturate(145%);
  border-radius: 44rpx;
  box-shadow: var(--shadow-glass);
  overflow: hidden;
  transition: transform 0.4s var(--apple-ease), box-shadow 0.4s var(--apple-ease);
  will-change: transform;
}

.float-subtle {
  animation: float-subtle 9.5s ease-in-out infinite alternate;
}

@keyframes float-subtle {
  0% { transform: translate3d(0, 0, 0); }
  100% { transform: translate3d(0, -12rpx, 0); }
}

.glass-card::before {
  content: "";
  position: absolute;
  top: 0; left: 0; right: 0;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.72), transparent);
  z-index: 1;
}

.animate-enter {
  animation: fade-in-up 1.2s var(--fluent-ease) both;
}
.delay-1 { animation-delay: 0.1s; }
.delay-2 { animation-delay: 0.2s; }
.delay-3 { animation-delay: 0.3s; }
.delay-4 { animation-delay: 0.4s; }

@keyframes fade-in-up {
  0% {
    opacity: 0;
    transform: translateY(40px) scale(0.98);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

.container {
  position: relative;
  z-index: 10;
  padding: 40rpx 32rpx;
}

.title-primary {
  font-size: 68rpx;
  font-weight: 800;
  color: var(--ink-primary);
  letter-spacing: -0.04em;
}

.title-secondary {
  font-size: 38rpx;
  font-weight: 700;
  color: var(--ink-primary);
  letter-spacing: -0.02em;
}

.text-gradient {
  background: linear-gradient(120deg, #f1a3bc 0%, #f6b28d 48%, #8fcbe8 100%);
  background-size: 220% auto;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  animation: text-shine 12s linear infinite;
}

@keyframes text-shine {
  to { background-position: 220% center; }
}

.text-muted {
  color: var(--ink-secondary);
  font-size: 28rpx;
  line-height: 1.5;
  font-weight: 400;
}

.btn-row {
  display: flex;
  gap: 24rpx;
}

.btn {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100rpx;
  border-radius: 50rpx;
  font-size: 30rpx;
  font-weight: 600;
  letter-spacing: 0;
  border: none;
  transition: all 0.4s var(--apple-ease);
  padding: 0 40rpx;
}

.btn::after {
  display: none;
}

.btn:active {
  transform: scale(0.96);
  opacity: 0.8;
}

.btn-neon {
  background: linear-gradient(135deg, #f7c1cf 0%, #f7d0ac 100%);
  color: #5e4d5f;
  box-shadow: 0 14px 28px rgba(243, 154, 181, 0.22);
}

.btn-glass {
  background: rgba(255, 255, 255, 0.5);
  border: 1px solid rgba(255, 255, 255, 0.82);
  color: var(--ink-primary);
  backdrop-filter: blur(18px);
}

.flex-1 {
  flex: 1;
}

.glass-tag {
  padding: 10rpx 24rpx;
  border-radius: 40rpx;
  background: rgba(255, 255, 255, 0.62);
  border: 1px solid rgba(255, 255, 255, 0.86);
  color: var(--ink-primary);
  font-size: 24rpx;
  font-weight: 500;
  backdrop-filter: blur(8px);
  box-shadow: 0 10rpx 24rpx rgba(225, 201, 213, 0.16);
}

.empty-glass {
  padding: 60rpx 40rpx;
  text-align: center;
  border: 1px dashed rgba(197, 176, 191, 0.45);
  border-radius: 32rpx;
  color: var(--ink-secondary);
  font-size: 28rpx;
  background: rgba(255, 255, 255, 0.36);
}

.gradient-border-wrap {
  position: relative;
  border-radius: 46rpx;
  padding: 2px;
  background: linear-gradient(135deg, rgba(255, 216, 230, 0.95), rgba(246, 195, 159, 0.88), rgba(217, 239, 248, 0.95));
  background-size: 220% 220%;
  animation: border-flow 9s ease infinite alternate;
  box-shadow: 0 12px 28px rgba(232, 205, 215, 0.18), 0 0 0 1px rgba(255, 255, 255, 0.28);
}

@keyframes border-flow {
  0% { background-position: 0% 50%; }
  100% { background-position: 100% 50%; }
}

.gradient-border-wrap > .glass-card {
  border: none;
  height: 100%;
  border-radius: 44rpx;
}
