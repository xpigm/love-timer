/* pages/wall/wall.wxss */

.wall-poster-page {
  min-height: 100vh;
}

.wall-shell {
  display: flex;
  flex-direction: column;
  gap: 72rpx;
  padding-top: 60rpx;
  padding-bottom: 88rpx;
}

.wall-topline {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 24rpx;
}

.wall-tags {
  display: flex;
  gap: 16rpx;
  flex-wrap: wrap;
}

.wall-topside {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 12rpx;
}

.wall-special-tag {
  padding-right: 26rpx;
}

.wall-special-tag-seal,
.wall-special-tag-medal {
  background: linear-gradient(135deg, rgba(255, 248, 232, 0.92) 0%, rgba(255, 236, 205, 0.78) 100%);
  color: #9d6a2f;
}

.wall-special-tag-glow,
.wall-special-tag-spotlight {
  background: linear-gradient(135deg, rgba(255, 234, 242, 0.92) 0%, rgba(255, 244, 232, 0.78) 100%);
  color: #bd4d78;
}

.wall-special-tag-frost {
  background: linear-gradient(135deg, rgba(243, 251, 255, 0.94) 0%, rgba(228, 244, 251, 0.78) 100%);
  color: #4d88a0;
}

.wall-scene-chip {
  min-height: 56rpx;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 0 20rpx;
  font-size: 20rpx;
}

.wall-scene-chip-medal,
.wall-scene-chip-festival {
  background: linear-gradient(135deg, rgba(255, 248, 232, 0.9) 0%, rgba(255, 236, 205, 0.72) 100%);
  color: #9d6a2f;
}

.wall-scene-chip-rose,
.wall-scene-chip-soft {
  background: rgba(255, 255, 255, 0.76);
}

.wall-scene-chip-frost {
  background: linear-gradient(135deg, rgba(243, 251, 255, 0.94) 0%, rgba(228, 244, 251, 0.78) 100%);
  color: #4d88a0;
}

.wall-date {
  padding-top: 10rpx;
  font-size: 22rpx;
  line-height: 1.5;
  letter-spacing: 4rpx;
  color: var(--ink-tertiary);
  text-transform: uppercase;
  text-align: right;
}

.wall-kicker {
  font-size: 18rpx;
  letter-spacing: 5rpx;
  color: var(--ink-tertiary);
}

.wall-hero {
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  gap: 20rpx;
  padding: 18rpx 0 10rpx;
}

.wall-hero-eyebrow {
  padding: 10rpx 24rpx;
  border-radius: 999rpx;
  background: var(--scene-pill-bg);
  border: 1px solid var(--scene-card-border);
  font-size: 18rpx;
  line-height: 1;
  letter-spacing: 4rpx;
  color: var(--accent-ink);
  box-shadow: var(--shadow-soft);
}

.wall-nameplate {
  font-family: "Songti SC", "STSong", "PingFang SC", serif;
  font-size: 50rpx;
  line-height: 1.16;
  font-weight: 600;
  letter-spacing: -1rpx;
  color: var(--ink-primary);
  text-shadow: 0 10rpx 28rpx rgba(255, 255, 255, 0.34);
}

.wall-name-sep {
  display: inline-block;
  margin: 0 10rpx;
  color: rgba(125, 103, 120, 0.5);
}

.wall-hero-title {
  max-width: 620rpx;
  font-size: 42rpx;
  line-height: 1.28;
  font-weight: 600;
  color: var(--ink-primary);
}

.wall-hero-body {
  max-width: 620rpx;
  font-size: 28rpx;
  line-height: 1.72;
  color: var(--ink-secondary);
  letter-spacing: 0.3rpx;
}

.wall-hero-meta {
  font-size: 20rpx;
  line-height: 1.6;
  letter-spacing: 1.6rpx;
  color: var(--ink-tertiary);
}

.wall-hero-meta-dot {
  margin: 0 8rpx;
}

.wall-composer {
  position: relative;
  padding: 32rpx 28rpx 26rpx;
  border-radius: 44rpx;
  overflow: hidden;
  background:
    radial-gradient(circle at top right, var(--scene-card-tint) 0%, transparent 26%),
    radial-gradient(circle at bottom left, var(--scene-highlight) 0%, transparent 24%),
    linear-gradient(145deg, var(--glass-bg) 0%, var(--glass-quiet-bg) 100%);
  border: 1px solid var(--scene-card-border);
  box-shadow: var(--shadow-soft);
}

.wall-composer::after {
  content: "";
  position: absolute;
  inset: 0;
  background: linear-gradient(180deg, rgba(255, 255, 255, 0.1) 0%, transparent 28%, transparent 82%, rgba(255, 255, 255, 0.04) 100%);
  pointer-events: none;
}

.wall-composer-glow {
  position: absolute;
  border-radius: 50%;
  filter: blur(52rpx);
  pointer-events: none;
  opacity: 0.12;
  animation: wall-composer-ambient 10.8s ease-in-out infinite alternate;
}

.wall-composer-glow-a {
  width: 220rpx;
  height: 220rpx;
  top: -78rpx;
  right: -36rpx;
  background: rgba(243, 154, 181, 0.14);
}

.wall-composer-glow-b {
  width: 180rpx;
  height: 180rpx;
  bottom: -36rpx;
  left: 16rpx;
  background: rgba(142, 202, 230, 0.12);
  animation-delay: -3.8s;
}

.wall-composer-breathe {
  position: absolute;
  left: 50%;
  bottom: -12rpx;
  width: 88%;
  height: 120rpx;
  transform: translateX(-50%);
  border-radius: 999rpx;
  background: radial-gradient(circle, rgba(255, 232, 240, 0.28) 0%, rgba(255, 232, 240, 0.12) 38%, transparent 76%);
  filter: blur(28rpx);
  opacity: 0.72;
  animation: wall-composer-breathe 7.4s ease-in-out infinite;
  pointer-events: none;
}

.wall-composer-topline,
.wall-composer-title,
.wall-composer-copy,
.wall-author-row,
.wall-composer-input,
.wall-composer-actions,
.wall-composer-helper {
  position: relative;
  z-index: 1;
}

.wall-composer-topline {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 18rpx;
}

.wall-composer-kicker {
  font-size: 18rpx;
  letter-spacing: 3rpx;
  color: var(--ink-tertiary);
}

.wall-composer-counter {
  font-family: var(--display-font);
  font-size: 18rpx;
  color: var(--accent-ink);
  font-variant-numeric: tabular-nums lining-nums;
}

.wall-composer-scene {
  margin-top: 14rpx;
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 16rpx;
}

.wall-composer-scene-badge {
  flex-shrink: 0;
  min-height: 56rpx;
  padding: 0 22rpx;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border-radius: 999rpx;
  background: var(--scene-pill-bg);
  border: 1px solid var(--scene-card-border);
  color: var(--accent-ink);
  font-size: 20rpx;
}

.wall-composer-scene-copy {
  font-size: 20rpx;
  line-height: 1.58;
  color: var(--ink-tertiary);
  text-align: right;
}

.wall-composer-title {
  margin-top: 16rpx;
  font-size: 30rpx;
  line-height: 1.34;
  font-weight: 600;
  color: var(--ink-primary);
}

.wall-composer-copy {
  margin-top: 12rpx;
  font-size: 24rpx;
  line-height: 1.68;
  color: var(--ink-secondary);
}

.wall-author-row {
  margin-top: 24rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18rpx;
}

.wall-author-left {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.wall-author-avatar-wrap {
  position: relative;
  width: 72rpx;
  height: 72rpx;
  padding: 0;
  margin: 0;
  background: none;
  border: none;
  line-height: 1;
  overflow: visible;
}

.wall-author-avatar-wrap::after {
  display: none;
}

.wall-author-avatar,
.wall-author-avatar-placeholder {
  width: 72rpx;
  height: 72rpx;
  border-radius: 50%;
  background: var(--glass-strong-bg);
  border: 1px solid var(--glass-border);
  box-shadow: var(--shadow-soft);
}

.wall-author-avatar-placeholder {
  background: var(--glass-quiet-bg);
}

.wall-author-avatar-sync {
  position: absolute;
  right: -4rpx;
  bottom: -4rpx;
  width: 32rpx;
  height: 32rpx;
  border-radius: 50%;
  background: var(--neon-primary);
  display: flex;
  align-items: center;
  justify-content: center;
  border: 2px solid var(--glass-strong-bg);
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.1);
  z-index: 2;
}

.sync-icon {
  width: 16rpx;
  height: 16rpx;
  border: 2rpx solid #fff;
  border-top-color: transparent;
  border-radius: 50%;
}

.wall-author-label {
  font-size: 20rpx;
  color: var(--ink-tertiary);
}

.wall-author-right {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 12rpx;
}

.wall-author-picker {
  flex-shrink: 0;
}

.wall-author-chip {
  min-width: 110rpx;
  height: 68rpx;
  padding: 0 20rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 999rpx;
  background: var(--glass-strong-bg);
  border: 1px solid var(--glass-border);
  color: var(--ink-primary);
  font-size: 22rpx;
  box-shadow: var(--shadow-soft);
}

.wall-author-input {
  flex: 1;
  width: 100%;
  height: 68rpx;
  padding: 0 32rpx;
  border-radius: 999rpx;
  background: var(--glass-quiet-bg);
  border: 1px solid var(--glass-border);
  color: var(--ink-primary);
  font-size: 24rpx;
  text-align: right;
}

.wall-composer-input {
  width: 100%;
  min-height: 260rpx;
  margin-top: 18rpx;
  padding: 24rpx 24rpx 22rpx;
  border-radius: 30rpx;
  background: var(--glass-strong-bg);
  border: 1px solid var(--glass-border);
  box-shadow: inset 0 1px 0 var(--glass-border-highlight);
  color: var(--ink-primary);
  font-size: 28rpx;
  line-height: 1.7;
}

.wall-input-placeholder {
  color: var(--ink-tertiary);
}

.wall-composer-actions {
  margin-top: 20rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 20rpx;
}

.wall-composer-link {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 60rpx;
  padding: 0 22rpx;
  border-radius: 999rpx;
  background: var(--glass-quiet-bg);
  border: 1px solid var(--glass-border);
  font-size: 22rpx;
  color: var(--accent-ink);
}

.wall-save-btn {
  min-width: 220rpx;
  height: 80rpx;
  padding: 0 30rpx;
  font-size: 24rpx;
}

.wall-composer-helper {
  margin-top: 16rpx;
  font-size: 20rpx;
  line-height: 1.58;
  color: var(--ink-tertiary);
}

.wall-stream {
  display: flex;
  flex-direction: column;
  gap: 0;
}

.wall-stream-topline {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 24rpx;
  margin-bottom: 24rpx;
}

.wall-stream-kicker {
  font-size: 18rpx;
  letter-spacing: 3rpx;
  color: var(--ink-tertiary);
}

.wall-stream-title {
  margin-top: 10rpx;
  font-size: 32rpx;
  line-height: 1.32;
  font-weight: 600;
  color: var(--ink-primary);
}

.wall-stream-meta {
  padding-top: 8rpx;
  font-size: 20rpx;
  color: var(--ink-tertiary);
  text-align: right;
}

.wall-stream-list {
  display: flex;
  flex-direction: column;
  gap: 22rpx;
}

.wall-note-item {
  position: relative;
  padding: 0 0 22rpx 22rpx;
}

.wall-note-item::before {
  content: "";
  position: absolute;
  left: 0;
  top: 12rpx;
  width: 6rpx;
  height: 6rpx;
  border-radius: 50%;
  background: var(--scene-highlight);
}

.wall-note-item::after {
  content: "";
  position: absolute;
  left: 2rpx;
  top: 30rpx;
  bottom: 0;
  width: 1px;
  background: linear-gradient(180deg, var(--scene-highlight) 0%, rgba(243, 154, 181, 0.04) 72%, transparent 100%);
}

.wall-note-item:last-child {
  padding-bottom: 0;
}

.wall-note-item:last-child::after {
  display: none;
}

.wall-note-item.is-latest::before {
  width: 8rpx;
  height: 8rpx;
  background: var(--neon-primary);
  box-shadow: 0 0 14rpx var(--scene-highlight);
}

.wall-note-header {
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin-bottom: 8rpx;
}

.wall-note-avatar {
  width: 36rpx;
  height: 36rpx;
  border-radius: 50%;
  background: var(--glass-quiet-bg);
  border: 1px solid var(--glass-border);
}

.wall-note-meta {
  font-size: 20rpx;
  color: var(--ink-tertiary);
}

.wall-note-content {
  margin-top: 10rpx;
  font-size: 28rpx;
  line-height: 1.72;
  color: var(--ink-primary);
  white-space: pre-wrap;
}

.wall-note-actions {
  margin-top: 14rpx;
  display: flex;
  gap: 22rpx;
}

.action-link {
  display: inline-flex;
  align-items: center;
  min-height: 56rpx;
  padding-right: 18rpx;
  font-size: 22rpx;
  color: var(--ink-secondary);
}

.action-link.danger {
  color: #bd5574;
}

.wall-stream-empty {
  padding: 42rpx 34rpx;
  border-radius: 34rpx;
  border: 1px dashed var(--scene-card-border);
  background: var(--empty-state-bg);
  box-shadow: var(--shadow-soft);
}

.wall-stream-empty-title {
  font-size: 26rpx;
  line-height: 1.62;
  color: var(--ink-secondary);
}

.wall-stream-empty-helper {
  margin-top: 10rpx;
  font-size: 22rpx;
  line-height: 1.6;
  color: var(--ink-tertiary);
}

@keyframes wall-composer-ambient {
  0% {
    transform: translate3d(0, 0, 0) scale(0.92);
    opacity: 0.08;
  }
  100% {
    transform: translate3d(10rpx, -10rpx, 0) scale(1.08);
    opacity: 0.16;
  }
}

@keyframes wall-composer-breathe {
  0%, 100% {
    opacity: 0.42;
    transform: translateX(-50%) scaleX(0.92) scaleY(0.92);
  }
  50% {
    opacity: 0.72;
    transform: translateX(-50%) scaleX(1.04) scaleY(1.03);
  }
}

@media (prefers-color-scheme: dark) {
  .wall-nameplate {
    text-shadow: 0 10rpx 30rpx rgba(0, 0, 0, 0.28);
  }

  .wall-composer {
    background:
      radial-gradient(circle at top right, rgba(255, 120, 173, 0.13), transparent 26%),
      radial-gradient(circle at bottom left, rgba(117, 216, 255, 0.12), transparent 24%),
      linear-gradient(145deg, var(--glass-bg) 0%, var(--glass-quiet-bg) 100%);
  }
}

.theme-midnight .wall-nameplate {
  text-shadow: 0 10rpx 30rpx rgba(0, 0, 0, 0.28);
}

.theme-midnight .wall-composer {
  background:
    radial-gradient(circle at top right, rgba(255, 120, 173, 0.13), transparent 26%),
    radial-gradient(circle at bottom left, rgba(117, 216, 255, 0.12), transparent 24%),
    linear-gradient(145deg, var(--glass-bg) 0%, var(--glass-quiet-bg) 100%);
}

.scene-anniversary .wall-composer-scene-badge,
.scene-milestone .wall-composer-scene-badge {
  background: linear-gradient(135deg, rgba(255, 248, 232, 0.94) 0%, rgba(255, 236, 205, 0.8) 100%);
  color: #9d6a2f;
}

.scene-valentine .wall-composer-scene-badge,
.scene-520 .wall-composer-scene-badge,
.scene-521 .wall-composer-scene-badge {
  border-radius: 999rpx;
}

.scene-christmas .wall-composer-scene-badge {
  background: linear-gradient(135deg, rgba(243, 251, 255, 0.94) 0%, rgba(228, 244, 251, 0.8) 100%);
  color: #4d88a0;
}

.scene-newyear .wall-nameplate,
.scene-newyear .wall-hero-title,
.scene-newyear .wall-hero-body,
.scene-newyear .wall-hero-meta,
.scene-newyear .wall-stream-title,
.scene-newyear .wall-stream-kicker {
  color: #f6edf8;
}

.scene-newyear .wall-note-content,
.scene-newyear .wall-stream-empty-title,
.scene-newyear .wall-stream-empty-helper,
.scene-newyear .wall-composer-copy,
.scene-newyear .wall-composer-helper {
  color: rgba(246, 237, 248, 0.84);
}
